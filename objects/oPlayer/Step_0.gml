// Inherit the parent event
event_inherited();
if (!global.reading) and (!global.cutscene){
estado = (player = global.player) ? estadoP : estadoN
estado()
}else{
    if(face != -1 and !global.cutscene){
        face = 0;
        vspd = 0;
        hspd = 0;
        change_sprite()
    }
}